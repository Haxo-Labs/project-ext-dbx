---
title: DBX – Backend-Agnostic Database API Server
layout: landing
showLogo: false
---

import { HomePage } from "vocs/components";

<HomePage.Root>
  <HomePage.Logo />
  <HomePage.Tagline>
    Backend-agnostic database API server with pluggable architecture.
  </HomePage.Tagline>
  <HomePage.InstallPackage name="dbx" type="install" />
  <HomePage.Description>
    DBX is a lightweight, high-performance database API server designed for edge and embedded systems. Built in
    Rust with pluggable backend adapters, providing REST and WebSocket APIs for multiple database types through a unified interface.
  </HomePage.Description>
  <HomePage.Buttons>
    <HomePage.Button href="/docs" variant="accent">
      Get started
    </HomePage.Button>
    <HomePage.Button href="https://github.com/effortlesslabs/dbx">GitHub</HomePage.Button>
  </HomePage.Buttons>
</HomePage.Root>

# DBX Documentation

Welcome to the DBX documentation! DBX is a lightweight, high-performance database API server with backend-agnostic architecture.

## What is DBX?

DBX is a minimal and portable HTTP/WebSocket proxy that exposes multiple database types through a unified API layer. Built in Rust with pluggable backend adapters implementing the `UniversalBackend` trait, DBX provides consistent APIs regardless of the underlying database.

### Key Features

- **Backend-Agnostic**: Pluggable adapter system for multiple database types
- **Unified Interface**: Consistent HTTP REST API and WebSocket support across all backends
- **TypeScript SDK**: Client library with backend capability detection
- **High Performance**: Built in Rust for maximum efficiency
- **Lightweight**: Minimal footprint, perfect for edge computing
- **Extensible**: Easy to add new database backends

### Supported Backends

#### Currently Available
- **Redis**: Complete implementation with strings, hashes, sets, sorted sets, bitmaps

#### Planned Backends
- **MongoDB**: Document-based operations with collections and aggregations
- **PostgreSQL**: Relational database with SQL query support
- **SQLite**: Embedded database for local storage

## Quick Start

### Using Docker

```bash
# Run with Redis backend
docker run -d --name dbx -p 3000:3000 \
  -e BACKEND_TYPE=redis \
  -e REDIS_URL=redis://localhost:6379 \
  effortlesslabs/dbx:latest

# Run with auto-detection
docker run -d --name dbx -p 3000:3000 \
  effortlesslabs/dbx:latest
```

### Using TypeScript SDK

```bash
# Install SDK
npm install dbx

# Use in your code
import { DbxClient } from "dbx";

const client = new DbxClient("http://localhost:3000");

// Data operations
await client.data.set("user:1", { name: "Alice", age: 30 });
const user = await client.data.get("user:1");

// Backend-specific operations (when available)
if (client.capabilities.supports("redis_strings")) {
  await client.redis.string.set("key", "value", 3600);
}
```

## Documentation Sections

### Getting Started

- **[Quick Start Guide](/docs/getting-started)** - Get up and running in minutes
- **[Installation](/docs/installation)** - Detailed installation instructions
- **[Configuration](/docs/configuration)** - Learn about multi-backend configuration
- **[Backend Setup](/docs/backends)** - Configure different database backends

### API Reference

#### Backend-Agnostic API

- **[Data Operations](/docs/api/universal/data)** - Data operations across backends
- **[Query Operations](/docs/api/universal/query)** - Cross-backend query capabilities
- **[Stream Operations](/docs/api/universal/stream)** - Stream and real-time operations
- **[Admin Operations](/docs/api/universal/admin)** - Server administration and health checks

#### Backend-Specific APIs

- **[Redis API](/docs/api/redis/)** - Redis-specific operations
- **[MongoDB API](/docs/api/mongodb/)** - MongoDB document operations (planned)
- **[PostgreSQL API](/docs/api/postgres/)** - SQL operations (planned)

#### WebSocket API

- **[Connection Guide](/docs/api/websocket/connection)** - WebSocket connection setup
- **[Backend-Agnostic Operations](/docs/api/websocket/universal)** - WebSocket operations
- **[Backend-Specific](/docs/api/websocket/backends)** - Backend-specific WebSocket operations

### SDK Documentation

#### TypeScript SDK

- **[Installation](/docs/sdk/typescript/installation)** - SDK setup and configuration
- **[Client Library](/docs/sdk/typescript/universal)** - Backend-agnostic operations
- **[Backend Clients](/docs/sdk/typescript/backends)** - Backend-specific clients
- **[Capability Detection](/docs/sdk/typescript/capabilities)** - Runtime capability detection

### Deployment

- **[Docker Deployment](/docs/deployment/docker)** - Deploy with Docker
- **[Multi-Backend Setup](/docs/deployment/multi-backend)** - Configure multiple backends
- **[Cloud Deployment](/docs/deployment/cloud)** - Deploy to cloud platforms

### Development

- **[Development Setup](/docs/development)** - Set up development environment
- **[Adding Backends](/docs/development/backends)** - Implement new database adapters
- **[Contributing](/docs/development/contributing)** - How to contribute to DBX

## API Endpoints Overview

### Backend-Agnostic Endpoints

| Endpoint              | Method | Description                    |
| --------------------- | ------ | ------------------------------ |
| `/data/{key}`         | GET    | Get data by key                |
| `/data/{key}`         | POST   | Set data by key                |
| `/data/{key}`         | DELETE | Delete data by key             |
| `/query`              | POST   | Execute query operation        |
| `/stream`             | POST   | Stream operation               |
| `/admin/health`       | GET    | Health check                   |
| `/admin/capabilities` | GET    | Backend capabilities discovery |

### Backend-Specific Endpoints

#### Redis (when Redis backend is active)

| Endpoint                          | Method | Description      |
| --------------------------------- | ------ | ---------------- |
| `/redis/string/{key}`             | GET    | Get string value |
| `/redis/string/{key}`             | POST   | Set string value |
| `/redis/hash/{key}/field/{field}` | GET    | Get hash field   |
| `/redis/set/{key}/members`        | GET    | Get set members  |

### WebSocket Endpoints

| Endpoint                     | Description       |
| ---------------------------- | ----------------- |
| `ws://localhost:3000/data`   | Data operations   |
| `ws://localhost:3000/query`  | Query operations  |
| `ws://localhost:3000/stream` | Stream operations |

## Architecture

### Core Components

- **dbx-core**: Backend traits and operation abstractions
- **dbx-adapter**: Database-specific implementations of UniversalBackend
- **dbx-config**: Multi-backend configuration management
- **dbx-router**: HTTP/WebSocket routing with backend abstraction

### UniversalBackend Trait

All database adapters implement a consistent interface:

```rust
#[async_trait]
pub trait UniversalBackend: Send + Sync {
    async fn execute_data(&self, operation: DataOperation) -> Result<DataResult, DbxError>;
    async fn execute_query(&self, operation: QueryOperation) -> Result<QueryResult, DbxError>;
    async fn execute_stream(&self, operation: StreamOperation) -> Result<StreamResult, DbxError>;
    fn capabilities(&self) -> BackendCapabilities;
}
```

## Use Cases

### Multi-Database Applications

Use DBX to provide a unified API across different database types in complex systems.

### Database Migration

Seamlessly migrate between database types without changing your application API.

### Edge Computing

Deploy DBX on edge nodes with local databases for reduced latency and improved resilience.

### IoT Applications

Use DBX in embedded systems to provide a standardized API for various storage backends.

### Microservices

Integrate DBX as a database proxy layer in microservice architectures for consistent data access patterns.

### Real-time Applications

Leverage WebSocket connections for real-time data synchronization and event streaming across different backends.

## Community

- **GitHub**: [effortlesslabs/dbx](https://github.com/effortlesslabs/dbx)
- **Issues**: [Report bugs and request features](https://github.com/effortlesslabs/dbx/issues)
- **Discussions**: [Join the community](https://github.com/effortlesslabs/dbx/discussions)

## License

DBX is open source and available under the MIT License.

# Features

## Performance

Ultra-low latency optimized for edge computing environments with high throughput and minimal overhead.

## Backend Support

Support for multiple database types through a pluggable adapter system with runtime capability detection.

## Connectivity

REST API and WebSocket connections with backend-agnostic TypeScript SDK.

## Edge-Optimized

Minimal Docker image designed for Cloudflare Workers, IoT devices, and microservices.

# Quick Start

```bash
# Run with auto-detection
docker run -d --name dbx -p 3000:3000 effortlesslabs/dbx:latest

# Use the TypeScript SDK
npm install dbx
```

# Community

- **GitHub**: [effortlesslabs/dbx](https://github.com/effortlesslabs/dbx)
- **Twitter**: [@effortlesslabs](https://x.com/effortlesslabs)
